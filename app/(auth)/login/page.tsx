import { LoginPreview } from "@/components/forms";

const Login = () => {
  return (
    <main className="h-screen m-4 flex items-center justify-center">
      <LoginPreview />
    </main>
  );
};

export default Login;
