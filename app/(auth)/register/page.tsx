import { RegisterPreview } from "@/components/forms";

const Register = () => {
  return (
    <main className="h-screen m-4 flex items-center justify-center">
      <RegisterPreview />
    </main>
  );
};

export default Register;
